fastapi>=0.110.0
uvicorn>=0.29.0
anthropic>=0.120.0
chromadb>=0.5.0
pydantic>=2.0.0
python-dotenv>=1.0.0
aiosqlite>=0.20.0
sentence-transformers>=3.0.0
sqlglot>=25.0.0
PyYAML>=6.0
a2ui-agent-sdk>=0.5.0
# a2ui-agent-sdk bumps opentelemetry-api/sdk, so the exporter must be bumped too
# (old exporter + new sdk breaks the chromadb import).
# ASCII-only comments on purpose: pip decodes requirements files with the system
# locale codec, so non-ASCII text breaks `pip install -r` on cp949/cp1252 shells.
opentelemetry-exporter-otlp-proto-grpc>=1.42.0
